# gitleaks config — extends the default ruleset with a TIGHT allowlist for
# protocol example and test fixture values (not real credentials):
#   - dGhlIHNhbXBsZSBub25jZQ==  RFC 6455 "the sample nonce" WebSocket key
#   - DGv6ra1nlYgDCS1FRnbzlw    Web Push test `auth` value
#   - test-secret-0123456789abcdef  Media image pipeline HMAC test fixture
# Allowlisting by exact value (not by broad path) keeps every other high-entropy
# string — including any real secret in a test file — still detected. Native
# secret-scanning + push-protection remain the first line of defense.
[extend]
useDefault = true

[[allowlists]]
description = "Protocol example values and deterministic HMAC fixtures in tests — not real secrets"
regexes = [
  '''dGhlIHNhbXBsZSBub25jZQ==''',
  '''DGv6ra1nlYgDCS1FRnbzlw''',
  '''test-secret-0123456789abcdef''',
]
