# Per-app release — delegates to the reusable workflow in golusoris/golusoris.
name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    uses: golusoris/golusoris/.github/workflows/release-go.yml@35a70086a4414758682cc4784d55190b05b9196e # main
    with:
      image-name: ghcr.io/${{ github.repository }}
    secrets: inherit
