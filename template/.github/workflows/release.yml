# Per-app release — delegates to the reusable workflow in golusoris/golusoris.
name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    uses: golusoris/golusoris/.github/workflows/release-go.yml@d3b1eacd632fa0e2fb281aa855e756bab9d14aff # main
    with:
      image-name: ghcr.io/${{ github.repository }}
    secrets: inherit
