# Per-app CI — delegates to the reusable workflow in golusoris/golusoris.
# Replace @main with a pinned tag (e.g. @v0.3.0) for stability.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: golusoris/golusoris/.github/workflows/ci-go.yml@db0bd2b28919e5cec3cb09c6182cc81eafd9fbf5 # main
    with:
      go-version-file: go.mod
      coverage-threshold: 70
      # Uncomment and set your Go module path to enable apidiff:
      # module-path: github.com/golusoris/app-CHANGEME

  codeql:
    uses: golusoris/golusoris/.github/workflows/codeql.yml@db0bd2b28919e5cec3cb09c6182cc81eafd9fbf5 # main

  scorecard:
    uses: golusoris/golusoris/.github/workflows/scorecard.yml@db0bd2b28919e5cec3cb09c6182cc81eafd9fbf5 # main
    secrets: inherit
