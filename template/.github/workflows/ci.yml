# Per-app CI — delegates to the reusable workflow in golusoris/golusoris.
# Replace @main with a pinned tag (e.g. @v0.3.0) for stability.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: golusoris/golusoris/.github/workflows/ci-go.yml@d3b1eacd632fa0e2fb281aa855e756bab9d14aff # main
    with:
      go-version-file: go.mod
      coverage-threshold: 70
      # Uncomment and set your Go module path to enable apidiff:
      # module-path: github.com/golusoris/app-CHANGEME

  codeql:
    uses: golusoris/golusoris/.github/workflows/codeql.yml@d3b1eacd632fa0e2fb281aa855e756bab9d14aff # main

  scorecard:
    uses: golusoris/golusoris/.github/workflows/scorecard.yml@d3b1eacd632fa0e2fb281aa855e756bab9d14aff # main
    secrets: inherit
