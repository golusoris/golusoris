# Per-app CI — delegates to the reusable workflow in golusoris/golusoris.
# Refs are pinned to the v0.4.0 release commit; Renovate bumps them.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: golusoris/golusoris/.github/workflows/ci-go.yml@3df9f1aecdd771e6709487fcd620e416fa0714d1 # v0.4.0
    with:
      go-version-file: go.mod
      coverage-threshold: 70
      # Uncomment and set your Go module path to enable apidiff:
      # module-path: github.com/golusoris/app-CHANGEME

  codeql:
    uses: golusoris/golusoris/.github/workflows/codeql.yml@3df9f1aecdd771e6709487fcd620e416fa0714d1 # v0.4.0

  scorecard:
    uses: golusoris/golusoris/.github/workflows/scorecard.yml@3df9f1aecdd771e6709487fcd620e416fa0714d1 # v0.4.0
    secrets: inherit
