# Per-app CI — delegates to the reusable workflow in golusoris/golusoris.
# Refs are pinned to the v0.4.0 release commit; Renovate bumps them.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: golusoris/golusoris/.github/workflows/ci-go.yml@b8303b789c658817226a9828fecb14660cc538ea # v0.7.0
    with:
      go-version-file: go.mod
      coverage-threshold: 70
      # Uncomment and set your Go module path to enable apidiff:
      # module-path: github.com/golusoris/app-CHANGEME

  codeql:
    uses: golusoris/golusoris/.github/workflows/codeql.yml@b8303b789c658817226a9828fecb14660cc538ea # v0.7.0

  scorecard:
    uses: golusoris/golusoris/.github/workflows/scorecard.yml@b8303b789c658817226a9828fecb14660cc538ea # v0.7.0
    secrets: inherit
